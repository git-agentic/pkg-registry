{
  "_comment": "Local fixture registry. `make-fixtures` packs each version's package/ dir into a .tgz and fills in integrity + sizes at build time. signatureStatus is declared here to simulate npm provenance.",
  "packages": {
    "leftpad-lite": {
      "class": "benign",
      "expect": { "verdict": "allow" },
      "versions": {
        "1.0.0": { "signatureStatus": "signed" },
        "1.0.1": { "signatureStatus": "signed" }
      }
    },
    "net-fetch-lite": {
      "class": "benign",
      "expect": { "verdict": "allow" },
      "versions": {
        "1.0.0": { "signatureStatus": "signed" },
        "1.0.1": { "signatureStatus": "signed" },
        "1.0.2": { "signatureStatus": "signed" }
      }
    },
    "color-stream": {
      "class": "malicious",
      "expect": { "1.4.0": "allow", "1.4.1": "block" },
      "versions": {
        "1.4.0": { "signatureStatus": "signed" },
        "1.4.1": { "signatureStatus": "unsigned" }
      }
    },
    "enforce-probe": {
      "class": "benign",
      "expect": { "verdict": "allow" },
      "versions": {
        "1.0.0": { "signatureStatus": "signed" }
      }
    }
  }
}
